// import React from 'react'

// import { useSTContext } from '@component/provider'

// import * as STElement from '@styled/index'

// export const WithError = (BaseComponent :  (p : unknown) => JSX.Element)  => (props : unknown) : JSX.Element => {
//   const { appState } = useSTContext()

//   // if (appState.error) {
//   //   return <STElement.STSubTitle className="padding">{ appState.error }</STElement.STSubTitle>
//   // }

//   return <BaseComponent 
//     {...props}
//   />
// }

import styled from 'styled-components';

const STApp = styled.div`

  ul {
    margin: 0;
    padding: 0;
    li {
      margin: 0;
      padding: 0
      list-style: none;
    }
  }
`;

STApp.displayName = 'STApp';

export { STApp };
